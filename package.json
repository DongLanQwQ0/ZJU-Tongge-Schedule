{
  "name": "tongge",
  "version": "1.0.0",
  "private": true,
  "description": "同格 · 找个课搭子一起上课 —— 局域网 Node 服务 + 纯前端，零 npm 依赖",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=18"
  }
}
