# 真实课表文件（含真实姓名），不入库。
# 用通配符而不是逐个写文件名 —— 逐个写的话，以后丢进来一个「张三.ics」
# 或者换个名字重新导出（「我的课表.ics」），git add -A 就照单全收了。
# 顺便也免得把同学的真名写进这个文件本身。
*.ics

# 运行时数据（账号、会话、群组）
/data/

# 部署包产物。这东西里面**含密码哈希和明文会话令牌**，
# 更不该进版本库 —— 一是没必要，二是真推上去就等同于把站点交出去了。
/dist/

# 依赖
node_modules/

# 系统与编辑器
Thumbs.db
Desktop.ini
.DS_Store
*.log
